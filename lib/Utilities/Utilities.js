/**
 * DECEPRECATED
 */
// exports.colour = (colour) => {
//     if (typeof colour === "string" && colour.startsWith("#")) {
//         const hex = colour.split("#")[1];
//         return parseInt(hex, 16);
//     } else {
//         return Number(colour);
//     }
// };